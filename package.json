{
  "name": "vitepress-project",
  "private": true,

  "scripts": {
    "deploy":"git add . && git commit -m \"update\" && git push && npm run build && npx gh-pages -d docs/.vitepress/dist",
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "serve": "vitepress serve docs"
  },
  "devDependencies": {
    "vitepress": "^0.22.4",
    "vue": "3.2.44"
  },
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "@algolia/client-search"
      ]
    }
  }
}
